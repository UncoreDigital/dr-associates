---

---

<style>
  .popup-overlay {
    position: fixed;
    inset: 0;
    background-color: rgba(0, 0, 0, 0.5);
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 1000;
    display: none; /* hidden by default */
  }

  .popup {
    background-color: #ffffff;
    width: 600px;
    max-width: 90%;
    border-radius: 12px;
    padding: 2rem;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
    position: relative;
    font-family: sans-serif;
  }

  .popup h2 {
    color: #71377e;
    font-size: 1.8rem;
    margin-bottom: 0.5rem;
  }

  .popup p {
    color: #685d9f;
    margin-bottom: 1.5rem;
  }

  .popup input[type="email"] {
    width: 100%;
    padding: 0.8rem;
    border: 2px solid #d7c0d8;
    border-radius: 6px;
    margin-bottom: 1rem;
    font-size: 1rem;
  }

  .popup button.subscribe {
    background-color: #72527f;
    color: #ffffff;
    padding: 0.8rem 1.2rem;
    border: none;
    border-radius: 6px;
    font-size: 1rem;
    cursor: pointer;
  }

  .popup button.subscribe:hover {
    background-color: #71377e;
  }

  .popup button.close {
    background: none;
    border: none;
    font-size: 1.5rem;
    color: #685d9f;
    position: absolute;
    top: 0.8rem;
    right: 1rem;
    cursor: pointer;
  }
</style>

<div class="popup-overlay" id="newsletter-popup">
  <div class="popup">
    <button class="close" id="popup-close">&times;</button>
    <h2>Subscribe to our Newsletter</h2>
    <p>Get the latest updates right in your inbox.</p>
    <form action="https://api.web3forms.com/submit" method="POST">
      <input
        type="hidden"
        name="access_key"
        value="bc8491c5-d1a9-4e3e-bc0d-b9e572b934d2"
      />
      <input
        type="email"
        name="email"
        placeholder="Enter your email"
        required
      />
      <button class="subscribe" type="submit">Subscribe</button>
    </form>
  </div>
</div>

<!-- <script is:inline>
  window.addEventListener("load", () => {
    const popup = document.getElementById("newsletter-popup");
    const closeBtn = document.getElementById("popup-close");
    const form = document.getElementById("newsletter-form");
    const emailInput = document.getElementById("newsletter-email");

    // Show popup after 1 second of page load
    setTimeout(() => {
      popup.style.display = "flex";
    }, 1000);

    closeBtn.addEventListener("click", () => {
      popup.style.display = "none";
    });

    form.addEventListener("submit", (e) => {
     e.preventDefault();
       alert(`Subscribed with: ${emailInput.value}`);
       popup.style.display = "none";
     });
  });
</script> -->

<script is:inline>
  window.addEventListener("DOMContentLoaded", () => {
    const popup = document.getElementById("newsletter-popup");
    const closeBtn = document.getElementById("popup-close");

    // Always start hidden
    popup.style.display = "none";

    let popupShown = false;
    const triggerDistance = window.innerHeight * 2.5; // 2.5x viewport height

    function handleScroll() {
      if (window.scrollY >= triggerDistance && !popupShown) {
        popup.style.display = "flex";
        popupShown = true;
        window.removeEventListener("scroll", handleScroll);
      }
    }

    window.addEventListener("scroll", handleScroll);

    closeBtn.addEventListener("click", () => {
      popup.style.display = "none";
    });
  });
</script>
